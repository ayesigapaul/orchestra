# Configuration is baked into the image, as everywhere in the local stack: one data source, Tempo.
FROM grafana/grafana:13.2.1
COPY datasources.yaml /etc/grafana/provisioning/datasources/datasources.yaml
